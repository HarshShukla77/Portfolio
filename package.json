{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Portfolio backend server",
  "main": "backend/server.js",
  
  "scripts": {
    "start": "node backend/Server.js",
    "dev": "nodemon backend/Server.js",
    "build": "cd Portfolio && npm install && npm run build && cp -r Portfolio/dist backend/public"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-async-handler": "^1.2.0",
    "mongoose": "^8.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}